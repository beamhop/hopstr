---
title: API Reference
---

# API Reference

The full, auto-generated API reference for every `@hopstr` package is built
from the source TSDoc with TypeDoc.

```bash
bun run docs:api      # generates docs/pages/api/generated from the packages
```

Once generated, browse it under [`/api/generated`](/api/generated). Each package's
public surface — types, functions, classes — is documented from the same source
the library ships, so it can never drift from the code.

## Packages at a glance

| Package | What it owns |
| --- | --- |
| `@hopstr/core` | event model, NIP-01 ids, schnorr, NIP-19/21/44, kernel helpers |
| `@hopstr/signers` | the async `Signer` — local / NIP-07 / NIP-46 / NIP-49 / NIP-06 |
| `@hopstr/relay` | one resilient relay connection (reconnect, NIP-11/42) |
| `@hopstr/pool` | many relays, dedup, async-iterable subscriptions |
| `@hopstr/router` | NIP-65 outbox routing |
| `@hopstr/store` | reactive event store (dedup, replaceable, NIP-09/40) |
| `@hopstr/nips` | every NIP, tree-shakeable |
| `@hopstr/client` | the high-level reactive `Nostr` client |
| `@hopstr/agent` | the drop-in `NostrClient` facade + autonomous loop |
